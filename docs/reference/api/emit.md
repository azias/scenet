# Emitting

Panel Core to SVG. The emitter makes no layout decisions: everything it draws was
already decided.

## `scenet.emit.svg`

```{automodule} scenet.emit.svg
```

## `scenet.emit.debug_svg`

```{automodule} scenet.emit.debug_svg
```

## `scenet.emit.strip`

```{automodule} scenet.emit.strip
```
