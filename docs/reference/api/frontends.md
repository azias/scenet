# Frontends

Text in, validated IR out. Two syntaxes, one IR -- which is what lets a comic script
and a YAML document compile identically without anything downstream knowing there is
more than one.

## `scenet.frontends.yaml_front`

```{automodule} scenet.frontends.yaml_front
```

## `scenet.frontends.script_front`

```{automodule} scenet.frontends.script_front
```
