# Compiling

The compiler itself. Source or IR in, Panel Core out.

Every entry point takes the same two optional keyword arguments -- `library` and
`metrics` -- so the character set and the font are both injectable, and neither is
read from the environment.

## `scenet.pipeline`

```{automodule} scenet.pipeline
```

## `scenet.compose`

```{automodule} scenet.compose
```
